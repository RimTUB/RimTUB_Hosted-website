import TransferElements from 'transfer-elements';

/*
new TransferElements({
  sourceElement: document.querySelector('.location'), // какой элемент надо перенести
  breakpoints: {
    1024: {
      targetElement: document.querySelector('.menu'), // куда надо перенести
      targetPosition: 1 // указать позицию перед каким элементом должно встать, по умолчанию 0
    }
  }
})
*/